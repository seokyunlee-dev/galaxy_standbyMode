<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== 권한 선언 ===== -->

    <!-- 인터넷 (날씨 API 등 향후 확장용) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 캘린더 읽기 -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- 알림 접근 (MediaSession 정보 획득을 위한 NotificationListenerService) -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- 화면 상시 켜짐은 코드에서 FLAG_KEEP_SCREEN_ON으로 처리하므로
         WAKE_LOCK은 선언만 해두면 충분합니다 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GalaxyStandbyMode"
        android:usesCleartextTraffic="true">

        <!-- ===== MainActivity ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== NotificationListenerService (음악 정보 획득) ===== -->
        <service
            android:name=".MediaNotificationListenerService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
